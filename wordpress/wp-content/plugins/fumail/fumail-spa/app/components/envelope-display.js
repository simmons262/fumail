import Ember from 'ember';

/*let funcs = {
	loadEnvelope : function() {
		var env = document.querySelectorAll('.env');
		var envelope = document.querySelector('.envelope-1');

		function addclass(){
		  envelope.style.pointerEvents= 'none';

		  for (var i = 0; i < env.length; i++) {
			env[i].classList.add("clicked");
		  }
		}
		envelope.addEventListener('click', addclass, false);
	}
};*/
export default Ember.Component.extend({
	//model() {
		//return funcs;
	//}
});
